export enum BottomTab {
  HOME = "Home",
  ATTENDANCE = "Attendance",
  CHECKOUT = "Check Out",
  FORM = "Form",
  SETTING = "Setting",
}

export enum Screen {
  HOME = "home",
  NOTIFICATION = "notification",
}
